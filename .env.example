# Database (leave empty to use SQLite)
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=crow
POSTGRES_USER=crow
POSTGRES_PASSWORD=

# App
PORT=3000
MEDIA_STORAGE_PATH=./uploads
SCHEDULER_POLL_INTERVAL_MS=15000
SCHEDULER_MAX_RETRIES=3

# Recurrent events (external URL fetched server-side and proxied to frontend)
RECURRENT_EVENTS_URL=http://localhost:3001/occurrences
